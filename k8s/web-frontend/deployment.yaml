apiVersion: apps/v1
kind: Deployment
metadata:
  name: web-frontend
  namespace: superstreaming
spec:
  replicas: 1
  selector:
    matchLabels:
      app: web-frontend
  template:
    metadata:
      labels:
        app: web-frontend
    spec:
      containers:
        - name: web-frontend
          image: superstreaming/web-frontend:latest
          ports:
            - containerPort: 80
          env:
            - name: READ_BASE_URLS
              value: "http://NODE_IP/whep/0/,http://NODE_IP/whep/1/,http://NODE_IP/whep/2/"
            - name: API_SERVER_URL
              value: "http://NODE_IP/api"
            - name: VIEWER_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: superstreaming-secrets
                  key: VIEWER_SECRET
          readinessProbe:
            httpGet:
              path: /
              port: 80
            initialDelaySeconds: 3
            periodSeconds: 10
          resources:
            requests:
              cpu: "50m"
              memory: "32Mi"
            limits:
              cpu: "200m"
              memory: "64Mi"
